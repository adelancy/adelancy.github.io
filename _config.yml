# Site metadata — the single source of truth for the site.
#
# scripts/gen-site-config.js compiles this into data/site.json (imported by the
# app) and scripts/gen-feed.js uses it to build public/feed.xml. Both run via
# `npm run gen` on predev/prebuild, so edit this file and restart the dev server.
#
# Kept in YAML at the repo root for historical reasons — the site itself is
# Next.js, not Jekyll.

title: Adrian Delancy
email: adelancy88@gmail.com
description: > # folded scalar: newlines below are collapsed into one line
  Software development engineering manager in Seattle, writing about
  software engineering, technical leadership, economics, geometry,
  and learning to fly.
baseurl: "" # subpath if the site is not served from the domain root
url: "https://adelancy.github.io" # base hostname & protocol, no trailing slash
github_username: adelancy
